version: 2.1

jobs:
  test:
    docker:
      - image: cimg/php:8.1.20
    steps:
      - checkout
      - store_test_results:
          path: .
      - run: php --version
      - run: composer --version

workflows:
  run_test:
    jobs:
      - test
